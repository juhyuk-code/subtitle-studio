[project]
name = "subtitle-studio-api"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.34,<1",
  "python-multipart>=0.0.20,<1",
  "httpx>=0.28,<1",
  "pydantic>=2.10,<3",
  "python-dotenv>=1.0,<2"
]

[project.optional-dependencies]
dev = ["pytest>=8.3,<9"]
desktop = [
  "faster-whisper>=1.2,<2",
  "nvidia-cublas-cu12>=12.8,<13; sys_platform == 'win32'",
  "pyannote.audio>=4,<5; sys_platform != 'darwin' or platform_machine != 'x86_64'",
  "pyannote.audio>=3.3,<4; sys_platform == 'darwin' and platform_machine == 'x86_64'",
  "torch==2.2.2; sys_platform == 'darwin' and platform_machine == 'x86_64'",
  "torchaudio==2.2.2; sys_platform == 'darwin' and platform_machine == 'x86_64'",
  "pyinstaller>=6.15,<7",
  "pywebview>=6.1,<7"
]

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
