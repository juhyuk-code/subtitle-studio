# Subtitle Studio: 첫 영상 클립 만들기

이 가이드는 처음 앱을 실행한 사용자가 한국어 영상에서 원하는 구간을
클립으로 만들고, 영어 자막을 입혀 내보내는 과정만 짧게 설명합니다.

## 전체 순서

**영상 불러오기 -> 타임스탬프 입력 -> 클립 탭 열기 -> 구간 설정 ->
한국어 전사 및 영어 번역 -> 자막 생성 -> 렌더링**

## 1. 처음 한 번만 설정하기

오른쪽 위의 톱니바퀴를 눌러 **Settings**를 엽니다.

1. **OpenRouter API key**를 입력합니다.
2. 교정, 번역, 포스트 카피에 사용할 모델을 선택합니다.
3. 완성된 파일을 저장할 **Video export folder**를 선택합니다.
4. 앱 글자가 작으면 **App font size**를 조절합니다.
5. 화자 구분이 필요하면 Hugging Face 토큰을 입력합니다.

모델은 제공사별로 볼 수 있으며, **Newest**에서 최근 모델을 확인할 수
있습니다. 이 설정은 앱을 껐다 켜도 유지됩니다.

## 2. 프로젝트와 영상 만들기

1. **New project**를 누릅니다.
2. 프로젝트 이름을 입력합니다.
3. 알고 있는 출연자 이름이 있으면 함께 입력합니다.
4. 프로젝트를 만든 뒤 영상 또는 오디오 파일을 불러옵니다.

지원 형식: MP4, MOV, MKV, MP3, WAV, M4A, AAC

첫 전사에서는 가장 큰 Whisper `large-v3` 모델을 내려받기 때문에 시간이
더 걸릴 수 있습니다. 다음부터는 저장된 모델을 다시 사용합니다.

## 3. 타임스탬프 입력하기

왼쪽 **Clips** 메뉴에 영상의 주요 시작 지점을 한 줄씩 붙여 넣습니다.

```text
00:00 오프닝
03:37 레버리지에 대한 이야기
12:48 시장 전망
18:20 마무리
```

`MM:SS`와 `HH:MM:SS` 형식을 사용할 수 있습니다.

여기서 타임스탬프는 **영상 이동용 표지**일 뿐입니다. 타임스탬프를
클릭하면 해당 위치로 이동하지만, 아직 편집용 클립이 만들어지지는
않습니다.

## 4. 편집할 클립 탭 열기

원하는 타임스탬프 오른쪽의 **화살표**를 누릅니다.

- 화면 아래에 해당 구간의 클립 탭이 열립니다.
- 한 번 연 클립은 Clips 메뉴에서 화살표 대신 원으로 표시됩니다.
- 각 탭은 구간, 전사, 자막 스타일, 포스트 카피가 분리된 독립 작업 공간입니다.
- 탭의 `X`를 누르면 닫을 수 있습니다. 닫아도 프로젝트가 삭제되지는 않습니다.

## 5. 클립 시작점과 끝점 정하기

클립 탭을 열면 타임라인이 해당 구간 중심으로 확대됩니다.

1. 파형을 클릭해 재생 커서를 원하는 위치에 놓습니다.
2. 정확한 시작 프레임에서 **Set in**을 누릅니다.
3. 정확한 끝 프레임에서 **Set out**을 누릅니다.
4. 또는 파형 위의 **IN / OUT 포인터**를 직접 드래그합니다.

IN 또는 OUT 포인터를 재생 커서 가까이 옮기면 커서 위치에 스냅됩니다.
재생 커서는 IN / OUT 위치에 강제로 스냅되지 않습니다.

> 클립 경계를 바꾸면 이전 전사와 자막은 삭제됩니다. 오디오 구간이
> 달라졌기 때문에 해당 클립을 다시 전사해야 합니다.

## 6. 한국어 전사부터 영어 번역까지 한 번에 실행하기

왼쪽 **Transcribe** 메뉴에서 **Create English transcript**를 누릅니다.

앱이 필요한 단계를 순서대로 실행합니다.

1. 화자 감지
2. Whisper `large-v3` 한국어 전사
3. 문맥 기반 한국어 교정
4. 이름과 용어 일관성 교정
5. 자연스러운 영어 번역

이미 완료된 단계는 건너뜁니다. 작업 중에는 예상 완료 시간이 표시됩니다.

- **Pause:** 현재 작업을 안전한 지점에서 일시 정지합니다.
- **Resume:** 일시 정지한 작업을 이어서 실행합니다.
- **Stop:** 작업을 완전히 취소합니다. 취소한 작업은 다시 시작해야 합니다.

## 7. 전사 내용 확인하고 수정하기

화면 아래 Transcript에서 각 대사를 확인합니다.

- 행을 클릭하면 영상이 해당 시점으로 이동합니다.
- 영상 재생 중에는 현재 대사가 보이도록 전사 영역만 자동 스크롤됩니다.
- **Raw Korean:** Whisper 원문이며 비교용으로 유지됩니다.
- **Corrected Korean:** 수정 가능한 최종 한국어입니다.
- **Natural English:** 수정 가능한 영어 번역입니다.
- 입력칸 바깥을 클릭하면 수정 내용이 저장됩니다.
- 화자 선택 메뉴에서 잘못 지정된 화자를 바꿀 수 있습니다.
- 자물쇠는 다시 생성할 때 해당 행을 보호합니다.
- 체크 표시는 검토 완료 상태입니다.
- **Review flags**는 인식 확률이 낮거나 확인이 필요한 행만 보여줍니다.

## 8. 화면에 표시될 자막 만들기

왼쪽 **Style** 메뉴를 엽니다.

1. **Words per line**에서 한 줄의 목표 단어 수를 정합니다.
2. **Caption lines**에서 한 자막에 표시할 최대 줄 수를 정합니다.
3. 처음이라면 **Generate captions**를 누릅니다.
4. 전사 내용을 수정했다면 **Regenerate captions**를 누릅니다.

Words per line과 Caption lines를 바꾸면 전사 문장을 새로운 자막 단위와
타임스탬프로 다시 나눕니다. 글자 크기를 자동으로 줄이지는 않습니다.
설정한 글자 크기로 영상 폭에 들어가지 않는 경우 실제 들어갈 수 있는 단어
수가 우선됩니다.

같은 메뉴에서 글꼴, 자막 크기, 자간, 줄 간격, 위치, 최대 폭, 배경색,
투명도, 여백, 테두리, 그림자를 조절할 수 있습니다.

- **Apply to all:** 현재 클립 스타일을 프로젝트의 모든 클립에 적용합니다.
- **Style preset:** 자주 쓰는 스타일을 이름으로 저장해 다른 프로젝트에서도 사용합니다.

## 9. 포스트 카피 만들기 (선택 사항)

왼쪽 **Post copy** 메뉴에서 현재 클립의 영어 전사를 바탕으로 게시물 문구를
생성할 수 있습니다.

- 제목과 인용문은 직접 수정할 수 있습니다.
- **Regenerate**를 누르면 새 버전을 만듭니다.
- 위쪽 **Copy** 버튼을 누르면 전체 문구가 클립보드에 복사됩니다.

## 10. 영상 또는 자막 파일 내보내기

1. 클립 탭 오른쪽의 **Render queue**를 누릅니다.
2. 내보낼 다른 클립도 같은 방법으로 큐에 추가합니다.
3. 화면 오른쪽 위의 **Render queue**를 엽니다.
4. 내보낼 클립과 형식을 선택합니다.

선택 가능한 형식:

- **Video:** 자막이 포함된 MP4
- **SRT:** 선택한 클립별 SRT 파일
- **Styled ASS:** 스타일이 포함된 클립별 ASS 파일

영상 없이 자막 파일만 필요하면 Video 선택을 끄고 SRT 또는 Styled ASS만
선택합니다.

기본 설정은 1080p, 최대 화질, GPU 인코딩입니다. GPU 인코딩을 사용할 수
없는 경우 CPU로 자동 전환됩니다. 완성된 파일은 Settings에서 지정한 폴더에
바로 저장되며 추가 하위 폴더는 만들지 않습니다.

내보내기가 완료되면 완료 메시지가 표시되고, 성공한 클립은 렌더 큐에서
자동으로 빠집니다.

## 자주 사용하는 단축키

텍스트 입력칸을 편집 중일 때는 재생 단축키가 작동하지 않습니다.

| 단축키 | 기능 |
| --- | --- |
| `Space` | 재생 / 일시 정지 |
| `Ctrl+Space` | 현재 클립의 IN 지점부터 재생 |
| `J` | 0.5배속으로 재생 |
| `K` | 1배속으로 재생 |
| `L` | 2배속으로 재생 |
| 파형 클릭 | 해당 위치로 재생 커서 이동 |
| 재생 커서 드래그 | 원하는 위치로 스크럽 |
| `Alt+마우스 휠` | 커서 위치를 중심으로 타임라인 확대 / 축소 |
| `Ctrl+마우스 휠` | 확대된 타임라인 좌우 이동 |
| `왼쪽 / 오른쪽 방향키` | 파형에 포커스가 있을 때 1프레임 이동 |
| `Shift+왼쪽 / 오른쪽 방향키` | 파형에 포커스가 있을 때 10프레임 이동 |

## 화자 감지를 더 정확하게 하는 방법

세 명이 출연하는 영상이라면 Transcribe 메뉴의 **Expected speakers**를
`3`으로 설정한 뒤 화자 감지를 실행합니다.

정기 출연자는 **Settings > Voices**에서 목소리 샘플을 등록할 수 있습니다.

- 최소 5초
- 권장 30~90초
- 최대 10분
- 음악이나 다른 사람 목소리가 없는 깨끗한 단독 음성 권장

목소리 샘플이 있어도 결과를 최종 확인하는 것이 좋습니다.

## 문제가 생겼을 때

### Create English transcript 버튼을 누를 수 없음

영상이 등록되어 있는지, 클립 탭을 열었는지, OpenRouter 키가 저장되어
있는지 확인합니다. 화자 감지를 사용하는 경우 Hugging Face 토큰도 필요합니다.

### 자막 설정이 내보낸 영상에 반영되지 않음

자막이 stale 상태인지 확인하고 **Regenerate captions**를 누릅니다. 전사를
수정하거나 Apply to all을 사용한 뒤에는 자막을 다시 생성해야 할 수 있습니다.

### Render 버튼을 누를 수 없음

최소 한 개의 클립이 렌더 큐에 있어야 하며, Video, SRT, Styled ASS 중
한 가지 이상을 선택해야 합니다. 자막도 최신 상태여야 합니다.

### 앱을 다시 켰더니 작업 위치가 바뀜

Subtitle Studio는 마지막 프로젝트, 클립 탭, 재생 위치, 배속, 타임라인 확대,
앱 글자 크기, 사이드바 폭을 자동 저장합니다. 상단에 **Saved locally**가
표시된 뒤 앱을 종료합니다.

## 첫 클립 완료 체크리스트

- [ ] OpenRouter 키와 모델을 설정했다.
- [ ] 내보내기 폴더를 선택했다.
- [ ] 영상을 불러왔다.
- [ ] 타임스탬프를 입력했다.
- [ ] 화살표를 눌러 클립 탭을 열었다.
- [ ] IN / OUT 경계를 정확히 정했다.
- [ ] Create English transcript를 실행했다.
- [ ] 한국어와 영어 전사를 검토했다.
- [ ] 자막 줄 수와 단어 수를 정하고 자막을 생성했다.
- [ ] 미리보기에서 자막 스타일을 확인했다.
- [ ] 클립을 Render queue에 추가했다.
- [ ] 출력 형식을 선택하고 내보냈다.
